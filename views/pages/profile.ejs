<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.0.3/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-900 text-white">
    <!-- Profile Container -->
    <div class="container mx-auto mt-8 p-8 bg-gray-800 rounded-lg shadow-lg">
        <div class="flex items-center">
            <img src="<%= user.avatar %>" alt="User Avatar" class="w-24 h-24 rounded-full border-4 border-gray-600">
            <div class="ml-6">
                <h1 class="text-3xl font-semibold">
                    <%= user.name %>
                </h1>
                <p class="text-gray-400">
                    <%= user.email %>
                </p>
                <p class="mt-2 text-gray-400">Joined: <%= user.joinDate %>
                </p>
            </div>
        </div>

        <!-- Profile Details -->
        <div class="mt-8">
            <h2 class="text-2xl font-semibold">Profile Details</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mt-4">
                <div class="bg-gray-700 p-6 rounded-lg">
                    <h3 class="text-xl font-semibold">About Me</h3>
                    <p class="mt-2 text-gray-400">
                        <%= user.aboutMe %>
                    </p>
                </div>
                <div class="bg-gray-700 p-6 rounded-lg">
                    <h3 class="text-xl font-semibold">Contact Information</h3>
                    <p class="mt-2 text-gray-400">Phone: <%= user.phone %>
                    </p>
                    <p class="mt-2 text-gray-400">Location: <%= user.location %>
                    </p>
                </div>
            </div>
        </div>
    </div>

</body>

</html>