<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <link href="/assets/styles/tailwind.css" rel="stylesheet">
    <!-- Toastify CSS -->
    <!-- <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css"> -->
    <!-- Toastify JS -->
    <script src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .toastify {
            border-radius: 12px;
            padding: 12px 20px;
            font-size: 14px;
            font-family: 'Roboto', sans-serif;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease;
        }
    </style>
</head>

<body class="bg-gray-100 dark:bg-gray-900 dark:text-white flex items-center justify-center min-h-screen">
    <div class="bg-white dark:bg-gray-800 p-8 rounded-lg shadow-lg w-96">
        <h2 class="text-2xl font-semibold text-center mb-6">Register</h2>

        <!-- Dynamically Show Toast if message exists -->
        <% if (message) { %>
            <script>
                Toastify({
                    text: "<%= message %>",
                    duration: 3000,
                    // gravity: "top",
                    // position: "right",
                    className: "<%= error ? 'font-bold fixed top-5 right-5 bg-red-500 text-white shadow-lg' : 'font-bold fixed top-5 right-5 bg-green-500 text-white shadow-lg' %>",
                    stopOnFocus: true,
                    // close: false,
                }).showToast();
            </script>
            <% } %>

                <form  method="POST" action="/auth/userRegister">
                    <div class="mb-4">
                        <label for="username"
                            class="block text-sm font-semibold text-gray-700 dark:text-gray-300">Username</label>
                        <input type="text" name="username" id="username"
                            class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-lg mt-2 bg-gray-50 dark:bg-gray-700 text-gray-900 dark:text-white"
                            required>
                    </div>

                    <div class="mb-4">
                        <label for="email"
                            class="block text-sm font-semibold text-gray-700 dark:text-gray-300">Email</label>
                        <input type="email" name="email" id="email"
                            class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-lg mt-2 bg-gray-50 dark:bg-gray-700 text-gray-900 dark:text-white"
                            required>
                    </div>

                    <div class="mb-4">
                        <label for="password"
                            class="block text-sm font-semibold text-gray-700 dark:text-gray-300">Password</label>
                        <input type="password" name="password" id="password"
                            class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-lg mt-2 bg-gray-50 dark:bg-gray-700 text-gray-900 dark:text-white"
                            required>
                    </div>

                    <div class="mb-4">
                        <label for="confirmPassword"
                            class="block text-sm font-semibold text-gray-700 dark:text-gray-300">Confirm
                            Password</label>
                        <input type="password" name="confirmPassword" id="confirmPassword"
                            class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-lg mt-2 bg-gray-50 dark:bg-gray-700 text-gray-900 dark:text-white"
                            required>
                    </div>

                    <button type="submit" 
                        class="w-full bg-blue-500 text-white py-2 rounded-lg mt-4 hover:bg-blue-600 focus:outline-none">Register</button>
                </form>

                <p class="mt-4 text-center text-md">
                    Have an account? <a href="/login" class="text-blue-500 hover:text-blue-700 font-bold">Sign In</a>
                </p>
    </div>
</body>

</html>